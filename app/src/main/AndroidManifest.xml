<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.sarthi">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name="androidx.multidex.MultiDexApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Sarthi">
        <activity android:name=".NewSignup"></activity>
        <activity android:name=".Attendant_Home" />
        <activity android:name=".Driver_Home" />
        <activity android:name=".User_Home" />
        <activity android:name=".verifyo_otp" />
        <activity android:name=".SignUpAttendant" />
        <activity android:name=".SignUpDriver" />
        <activity android:name=".SignUpUser" />
        <activity android:name=".login_signup" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>